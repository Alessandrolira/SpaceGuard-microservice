package com.example.spaceguard.domain.risco;

import com.example.spaceguard.domain.user.User;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RiscoReposiroty extends JpaRepository<Risco, String> {
}
