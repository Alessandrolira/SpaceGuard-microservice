.claude
./claude
/.claude
/claude

### env (NUNCA commitar credenciais) — vale para qualquer subpasta ###
.env
.env.local
.env.*.local

### Build Maven ###
target/

### Deploy ACI gerado (CONTEM SEGREDOS — nunca versionar) ###
scripts/aci-deployment.yaml
